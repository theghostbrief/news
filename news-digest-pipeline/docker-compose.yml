version: '3.8'
services:
  news-digest:
    build: .
    container_name: news-digest-pipeline
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./output:/app/output
      # prompts/config.md are editable from the Settings UI (writes go here),
      # so this mount must be read-write — a :ro mount makes content/prompt
      # saves fail with EROFS ("Failed to save settings").
      - ./prompts:/app/prompts
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.news-digest.rule=Host(`theghostbrief.com`) || Host(`www.theghostbrief.com`)"
      - "traefik.http.routers.news-digest.entrypoints=web,websecure"
      - "traefik.http.routers.news-digest.tls=true"
      - "traefik.http.routers.news-digest.tls.certresolver=mytlschallenge"
      - "traefik.http.services.news-digest.loadbalancer.server.port=3000"
    networks:
      - traefik

networks:
  traefik:
    external: true
    name: root_default
